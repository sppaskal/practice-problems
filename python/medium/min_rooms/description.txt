You’re given n half-open intervals [start, end) representing meetings. Return the minimum number of rooms required so none overlap in the same room.

Constraints: 1 ≤ n ≤ 2e5, times are integers, not sorted. If a meeting ends at t and another starts at t, they don’t overlap.
